<nav class="Pagination" role="navigation">
    {{#if prev}}
        <a class="Pagination-newerPosts" href="{{page_url prev}}">&larr; Newer Posts</a>
    {{/if}}
    <span class="Pagination-pageNumber">Page {{page}} of {{pages}}</span>
    {{#if next}}
        <a class="Pagination-olderPosts" href="{{page_url next}}">Older Posts &rarr;</a>
    {{/if}}
</nav>
