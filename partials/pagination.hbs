<nav class="Pagination" role="navigation">
    {{#if prev}}
        <a class="Pagination-newerPosts" href="{{page_url prev}}">&larr; Recientes</a>
    {{/if}}
    <span class="Pagination-pageNumber">Página {{page}} de {{pages}}</span>
    {{#if next}}
        <a class="Pagination-olderPosts" href="{{page_url next}}">Anteriores &rarr;</a>
    {{/if}}
</nav>
