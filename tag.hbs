{{!< default}}

<header class="tag-head MainHeader{{#if tag.feature_image}}" style="background-image: url({{tag.feature_image}}){{else}}{{#if @blog.cover_image}}" style="background-image: url({{@blog.cover_image}}){{else}}--noCover{{/if}}{{/if}}">
    <div class="MainHeader-content">
        <h1 class="MainHeader-title">{{tag.name}}</h1>
        <h2 class="MainHeader-description">
            {{#if tag.description}}
                {{tag.description}}
            {{else}}
                {{pagination.total}} artículos
            {{/if}}
        </h2>
    </div>
</header>

<main class="content" role="main">

    {{> "loop"}}

</main>
